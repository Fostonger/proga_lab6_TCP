//import queueManager.PriorityQueueManager;

public class Main {
    public static void main(String[] args) {
//        if (args.length < 1) {
//            System.out.println("filepath to json file should be given in launch arguments!");
//            return;
//        }
//        JSONDecoder decoder = new JSONDecoder();
//        JSONEncoder encoder = new JSONEncoder();
//        try {
//            CommandReader readerWriter = new CommandReader(System.in, System.out);
//            PropertiesReceiver receiver = new PropertiesReceiver(readerWriter, readerWriter);
//            RouteFactory routeFactory = new RouteFactory(receiver);
//            PriorityQueueManager queueManager = new PriorityQueueManager(decoder, encoder, args[0]);
//            CommandInterpreter interpreter = new CommandInterpreter(queueManager, routeFactory, readerWriter, readerWriter);
//            while (true) {
//                interpreter.fetchCommand();
//            }
//        } catch (JsonProcessingException e) {
//            System.out.println("given file doesn't contain needed value, try another one");
//        } catch (IOException e) {
//            System.out.println("the given file is not found or you don't have permissions to use it");
//        }
    }

}